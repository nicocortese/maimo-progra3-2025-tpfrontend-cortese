# Copiá este archivo como .env (o .env.local). Nunca se sube a git.

# URL del backend SIN "/api" al final (el código lo agrega).
# Si la cambiás, reiniciá "yarn dev" para que Next la tome.
NEXT_PUBLIC_API_URL="http://localhost:4000"
